/**
 * Restaurant portal shell.
 *
 * Realm: Hotel (restaurant). Phase 02 delivers the wiring only — no business page, no workflow
 * and no authoritative rule lives here (CLAUDE.md §3).
 */
export default function Page() {
  return (
    <main>
      <h1>Restaurant</h1>
      <p>Menu and order management for a linked restaurant.</p>
      <p>Realm: Hotel (restaurant)</p>
    </main>
  );
}
