/** Injection tokens for the public module's ports. */
export const PUBLIC_POOL = Symbol('public.pool');
export const PUBLIC_GEO = Symbol('public.geo');
export const PUBLIC_CATEGORY_HOLDS = Symbol('public.categoryHolds');
