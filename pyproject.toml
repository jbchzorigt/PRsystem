[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "prsystem-core"
version = "0.1.0"
description = "Hotel platform domain controls: expiry, cash reservations, settlement"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
